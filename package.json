{
  "scripts": {
    "build": "vite build",
    "capacitor:add": "npx cap add ios",
    "capacitor:sync": "npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8",
    "@capacitor/ios": "^5.7.8"
  }
}
